---
local_user_home: "{{ ansible_env.HOME }}"
base_clone_path: "{{ local_user_home }}/z/gl/openstack"
base_virtualenv_path: "{{ local_user_home }}/y/virtualenvs/osvenv"
openrc_dir: "{{ local_user_home }}/y/os-openrc"
admin_openrc_file: "{{ openrc_dir }}/admin-openrc.sh"
virtualenv_path: "{{ base_virtualenv_path }}/{{ version_release }}"
virtualenv_bin: "{{ virtualenv_path }}/bin"
virtualenv_activator: "{{ virtualenv_bin }}/activate"
virtualenv_python: "{{ virtualenv_bin }}/python"
